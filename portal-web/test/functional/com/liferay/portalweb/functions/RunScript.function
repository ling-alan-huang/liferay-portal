@default = "runScript"
definition {

	function runScript {
		WaitForSPARefresh.WaitForSPARefresh();

		selenium.runScript();

		selenium.assertJavaScriptErrors();

		selenium.assertLiferayErrors();
	}

}