@default = "isNotEditable"
definition {

	function isNotEditable {
		WaitForSPARefresh.WaitForSPARefresh();

		selenium.waitForElementPresent();

		selenium.waitForVisible();

		selenium.isNotEditable();
	}

}