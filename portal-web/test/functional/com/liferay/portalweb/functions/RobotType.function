@default = "robotType"
@summary = "Type to '${locator1}' the value '${value1}'"
definition {

	function robotType {
		WaitForSPARefresh.WaitForSPARefresh();

		selenium.robotType();
	}

	function robotTypeShortcut {
		WaitForSPARefresh.WaitForSPARefresh();

		selenium.robotTypeShortcut();
	}

}