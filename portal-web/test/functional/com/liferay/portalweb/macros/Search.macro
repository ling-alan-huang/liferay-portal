definition {

	macro clearSearchResult {
		Click.Click(locator1 = "Link#CLEAR");

		AssertElementNotPresent.AssertElementNotPresent(locator1 = "Search#SEARCH_INFO");
	}

	macro gotoResultAssetViaTitle {
		AssertClick.AssertClick(
			key_searchAssetTitle = "${searchAssetTitle}",
			locator1 = "SearchResults#ASSET_ENTRY_TABLE_TITLE",
			value1 = "${searchAssetTitle}");
	}

	macro openSearchPage {
		if (!(isSet(siteURLKey))) {
			var siteURLKey = "guest";
		}

		Navigator.openWithAppendToBaseURL(urlAppend = "web/${siteURLKey}/search");
	}

	macro searchCP {
		AssertElementPresent.AssertElementPresent(locator1 = "TextInput#SEARCH");

		AssertElementPresent.AssertElementPresent(locator1 = "Icon#BASIC_SEARCH");

		Type.Type(
			locator1 = "TextInput#SEARCH",
			value1 = "${searchTerm}");

		KeyPress.KeyPress(
			locator1 = "TextInput#SEARCH",
			value1 = "\RETURN");

		if (!(isSet(modal))) {
			WaitForLiferayEvent.initializeLiferayEventLog();
		}
	}

	macro searchPG {
		var key_portletName = "${portletName}";

		AssertElementPresent.AssertElementPresent(locator1 = "Portlet#SEARCH_FIELD");

		Type.Type(
			locator1 = "Portlet#SEARCH_FIELD",
			value1 = "${searchTerm}");

		KeyPress.KeyPress(
			locator1 = "Portlet#SEARCH_FIELD",
			value1 = "\RETURN");

		if (!(isSet(modal))) {
			WaitForLiferayEvent.initializeLiferayEventLog();
		}
	}

	macro viewNoResultAssetTitle {
		AssertElementNotPresent.AssertElementNotPresent(
			key_searchAssetTitle = "${searchAssetTitle}",
			locator1 = "SearchResults#ASSET_ENTRY_TABLE_TITLE");
	}

	macro viewNotPresentInContentBody {
		AssertClick.AssertClick(
			key_rowEntry = "${siteName}",
			locator1 = "ContentRow#ENTRY_CONTENT_ENTRY_NAME_LINK",
			value1 = "${siteName}");

		AssertElementNotPresent.AssertElementNotPresent(locator1 = "//body[.='${elementValue}']");
	}

	macro viewResultAssetTitle {
		AssertTextEquals.AssertTextEquals(
			key_searchAssetTitle = "${searchAssetTitle}",
			locator1 = "SearchResults#ASSET_ENTRY_TABLE_TITLE",
			value1 = "${searchAssetTitle}");
	}

}