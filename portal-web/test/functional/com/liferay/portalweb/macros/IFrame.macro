definition {

	macro addCP {
		SelectFrame.SelectFrame(locator1 = "IFrame#CONFIGURATION");

		Type.Type(
			locator1 = "TextInput#SOURCE_URL",
			value1 = "${sourceURL}");

		PortletEntry.save();

		SelectFrame.SelectFrame(value1 = "relative=top");
	}

	macro closeFrame {
		SelectFrame.SelectFrame(value1 = "relative=top");

		Click.mouseDownMouseUp(locator1 = "Modal#CLOSE_BUTTON");
	}

	macro saveConfiguration {
		PortletEntry.save();

		SelectFrame.SelectFrame(value1 = "relative=top");
	}

	macro selectCategoryFrame {
		SelectFrame.SelectFrame(locator1 = "IFrame#SELECT_CATEGORY");
	}

	macro selectConfigurationFrame {
		if (IsElementPresent.IsElementPresent(locator1 = "IFrame#MODAL_BODY")) {
			echo("Selecting configuration iframe");

			SelectFrame.SelectFrame(locator1 = "IFrame#MODAL_BODY");
		}
	}

	macro selectEditAssetFrame {
		SelectFrame.SelectFrame(locator1 = "IFrame#EDIT_ASSET");
	}

	macro selectModalFrame {
		SelectFrame.SelectFrame(locator1 = "IFrame#MODAL_IFRAME");
	}

	macro selectPermissionsFrame {
		SelectFrame.SelectFrame(
			key_title = "Permissions",
			locator1 = "IFrame#MODAL_ANY");
	}

	macro selectTagFrame {
		SelectFrame.SelectFrame(
			key_title = "Tags",
			locator1 = "IFrame#MODAL_ANY");
	}

	macro selectTopFrame {
		SelectFrame.SelectFrame(value1 = "relative=top");
	}

}