definition {

	macro addCategoriesFilter {
		Panel.expandPanel(panel = "Filter");

		if (isSet(queryAndOperator)) {
			Select.Select(
				locator1 = "APConfiguration#FILTER_QUERY_AND_OPERATOR",
				value1 = "${queryAndOperator}");
		}

		if (isSet(queryContains)) {
			Select.Select(
				locator1 = "APConfiguration#FILTER_QUERY_CONTAINS",
				value1 = "${queryContains}");
		}

		Select.Select(
			locator1 = "APConfiguration#FILTER_CATEGORIZATION_SELECT",
			value1 = "Categories");

		Click.Click(locator1 = "Button#SELECT_CATEGORIES");

		AssetCategorization.gotoSelectCategory();

		for (var categoryName : list "${categoryNameList}") {
			Portlet.selectTreeNode(nodeName = "${categoryName}");
		}

		SelectFrameTop.SelectFrameTop();

		Click.Click(locator1 = "Button#DONE");

		PortletEntry.save();
	}

	macro addDynamicSelection {
		if (isSet(depotName)) {
			LexiconEntry.gotoAddMenuItem(menuItem = "Dynamic Collection");
		}
		else if (IsVisible.IsVisible(locator1 = "Message#EMPTY_INFO_TAGLIB")) {
			Click.waitForMenuToggleJSClick(locator1 = "Dropdown#EMPTY_MESSAGE_DROPDOWN");

			Click.Click(
				key_menuItem = "Dynamic Collection",
				locator1 = "MenuItem#DROPDOWN_MENU_ITEM");
		}
		else {
			LexiconEntry.gotoAddMenuItem(menuItem = "Dynamic Collection");
		}

		PortletEntry.inputText(
			fieldLabel = "Title",
			text = "${assetListTitle}");

		PortletEntry.save();
	}

	macro addKeywordsFilter {
		Panel.expandPanel(panel = "Source");

		if (!(IsSelectedLabel.IsSelectedLabel(locator1 = "APConfiguration#SOURCE_SCOPE_ASSET_TYPE_SELECT", value1 = "All Types"))) {
			Select.Select(
				locator1 = "APConfiguration#SOURCE_SCOPE_ASSET_TYPE_SELECT",
				value1 = "All Types");
		}

		Panel.expandPanel(panel = "Filter");

		Select.Select(
			locator1 = "APConfiguration#FILTER_CATEGORIZATION_SELECT",
			value1 = "Keywords");

		Select.Select(
			locator1 = "APConfiguration#FILTER_QUERY_CONTAINS",
			value1 = "${queryContains}");

		Select.Select(
			locator1 = "APConfiguration#FILTER_QUERY_AND_OPERATOR",
			value1 = "${queryAndOperator}");

		Type.Type(
			locator1 = "AssetLists#ASSET_LIST_KEYWORDS_FIELD",
			value1 = "${keyWords}");

		PortletEntry.save();

		AssertTextEquals.AssertTextEquals(
			locator1 = "AssetLists#ASSET_LIST_KEYWORDS_FIELD",
			value1 = "${keyWords}");
	}

	macro addManualSelection {
		if (isSet(depotName)) {
			LexiconEntry.gotoAddMenuItem(menuItem = "Manual Collection");
		}
		else if (IsVisible.IsVisible(locator1 = "Message#EMPTY_INFO_TAGLIB")) {
			Click.waitForMenuToggleJSClick(locator1 = "Dropdown#EMPTY_MESSAGE_DROPDOWN");

			Click.Click(
				key_menuItem = "Manual Collection",
				locator1 = "MenuItem#DROPDOWN_MENU_ITEM");
		}
		else {
			LexiconEntry.gotoAddMenuItem(menuItem = "Manual Collection");
		}

		PortletEntry.inputText(
			fieldLabel = "Title",
			text = "${assetListTitle}");

		PortletEntry.save();
	}

	macro addPersonalizedVariation {
		if (IsElementPresent.IsElementPresent(locator1 = "AssetLists#ADD_VARIATION_ICON")) {
			Click.Click(locator1 = "AssetLists#ADD_VARIATION_ICON");
		}
		else {
			Button.click(button = "New Personalized Variation");
		}

		SelectFrame.SelectFrame(locator1 = "IFrame#MODAL_BODY");

		LexiconTable.clickTableEntryTitle(tableEntry = "${segmentName}");

		SelectFrameTop.SelectFrameTop();

		Alert.viewSuccessMessage();
	}

	macro addSelection {
		Click.Click(locator1 = "Button#NEW");

		MenuItem.click(menuItem = "${menuItem}");

		PortletEntry.inputText(
			fieldLabel = "Title",
			text = "${collectionName}");

		Button.clickSave();

		Select.Select(
			locator1 = "APConfiguration#SOURCE_SCOPE_ASSET_TYPE_SELECT",
			value1 = "${selectedAsset}");

		WaitForElementNotPresent.WaitForElementNotPresent(
			key_text = "Save",
			locator1 = "Button#ANY_DISABLED");

		PortletEntry.save();
	}

	macro addTagsFilter {
		Panel.expandPanel(panel = "Filter");

		if (isSet(queryAndOperator)) {
			Select.Select(
				locator1 = "APConfiguration#FILTER_QUERY_AND_OPERATOR",
				value1 = "${queryAndOperator}");
		}

		if (isSet(queryContains)) {
			Select.Select(
				locator1 = "APConfiguration#FILTER_QUERY_CONTAINS",
				value1 = "${queryContains}");
		}

		Select.Select(
			locator1 = "APConfiguration#FILTER_CATEGORIZATION_SELECT",
			value1 = "Tags");

		Click.Click(locator1 = "AssetCategorization#TAGS_SELECT_BUTTON");

		SelectFrame.SelectFrame(locator1 = "IFrame#MODAL_BODY");

		for (var tagName : list "${tagNameList}") {
			Check.Check(
				key_rowEntry = "${tagName}",
				locator1 = "ContentRow#ENTRY_CONTENT_ENTRY_CHECKBOX");
		}

		SelectFrameTop.SelectFrameTop();

		AssertClick.AssertClick(
			locator1 = "Button#DONE",
			value1 = "Done");

		PortletEntry.save();
	}

	macro configureFilterByField {
		Check.checkToggleSwitch(locator1 = "APConfiguration#SOURCE_SCOPE_FILTER_BY_FIELD_CHECKBOX");

		WaitForElementPresent.WaitForElementPresent(locator1 = "APConfiguration#SOURCE_SCOPE_FILTER_BY_FIELD_SELECT_BUTTON_ENABLED");

		Click.Click(locator1 = "APConfiguration#SOURCE_SCOPE_FILTER_BY_FIELD_SELECT_BUTTON");

		SelectFrame.SelectFrame(locator1 = "IFrame#MODAL_BODY");

		Check.Check(
			key_fieldName = "${fieldName}",
			locator1 = "APConfigurationFilterbyfield#FIELD_FILTER_RADIO_BUTTON");
	}

	macro configureItemTypeInDynamicCollection {
		Panel.expandPanel(panel = "Source");

		if (isSet(itemType)) {
			Select.Select(
				locator1 = "APConfiguration#SOURCE_SCOPE_ASSET_TYPE_SELECT",
				value1 = "${itemType}");
		}

		if (isSet(itemSubtype)) {
			Select.Select(
				locator1 = "APConfiguration#SOURCE_SCOPE_ASSET_SUBTYPE_SELECT",
				value1 = "${itemSubtype}");
		}

		WaitForElementNotPresent.WaitForElementNotPresent(
			key_text = "Save",
			locator1 = "Button#ANY_DISABLED");

		PortletEntry.save();
	}

	macro configureItemTypeInManualCollection {
		if (isSet(variationName)) {
			Navigator.gotoNavTab(navTab = "${variationName}");
		}

		if (isSet(itemType)) {
			Select.Select(
				locator1 = "APConfiguration#SOURCE_SCOPE_ASSET_TYPE_SELECT",
				value1 = "${itemType}");
		}

		if (isSet(itemSubtype)) {
			Select.Select(
				locator1 = "APConfiguration#SOURCE_SCOPE_ASSET_SUBTYPE_SELECT",
				value1 = "${itemSubtype}");
		}

		WaitForElementNotPresent.WaitForElementNotPresent(
			key_text = "Save",
			locator1 = "Button#ANY_DISABLED");

		PortletEntry.save();
	}

	macro configureScope {
		Click.Click(
			key_title = "${assetListTitle}",
			locator1 = "AssetLists#ASSET_LIST_ENTRY_TITLE");

		if (isSet(selectedAsset)) {
			Panel.expandPanel(panel = "Source");

			Select.Select(
				locator1 = "APConfiguration#SOURCE_SCOPE_ASSET_TYPE_SELECT",
				value1 = "${selectedAsset}");
		}

		Panel.expandPanel(panel = "Scope");

		if (isSet(otherSite)) {
			AssertClick.AssertClick(
				locator1 = "Configuration#SCOPE_SELECT",
				value1 = "Select");

			MenuItem.click(menuItem = "Other Site or Asset Library...");

			SelectFrame.SelectFrame(value1 = "relative=top");

			SelectFrame.SelectFrame(locator1 = "IFrame#SELECT_SITES");

			Navigator.gotoNavTab(navTab = "My Sites");

			LexiconEntry.changeDisplayStyle(displayStyle = "table");

			LexiconEntry.gotoEntry(rowEntry = "${otherSite}");
		}

		if (isSet(defaultSite)) {
			AssertClick.AssertClick(
				locator1 = "Configuration#SCOPE_SELECT",
				value1 = "Select");

			AssertClick.AssertClick(
				key_menuItem = "${defaultSite}",
				locator1 = "AssetLists#ASSET_LIST_SCOPE_ITEM",
				value1 = "${defaultSite}");
		}

		SelectFrameTop.SelectFrameTop();

		PortletEntry.save();
	}

	macro deleteEntry {
		LexiconList.delete(listEntry = "${assetListTitle}");

		Alert.viewSuccessMessage();
	}

	macro deleteManualSelectionAssets {
		for (var assetName : list "${assetsList}") {
			Click.Click(
				key_entryName = "${assetName}",
				locator1 = "LexiconTable#TABLE_ENTRY_DELETE");
		}
	}

	macro editAssetList {
		AssetListsAdmin.gotoEntryMenuItem(
			assetListTitle = "${assetListTitle}",
			item = "Edit");

		WaitForPageLoad.WaitForPageLoad();
	}

	macro enableCollectionsToCombineAssetsFromSegments {
		ApplicationsMenu.gotoPortlet(
			category = "Configuration",
			panel = "Control Panel",
			portlet = "System Settings");

		SystemSettings.gotoConfiguration(
			configurationCategory = "Assets",
			configurationName = "Collections",
			configurationScope = "System Scope");

		Click.javaScriptClick(
			checkboxName = "${collectionType}",
			locator1 = "Checkbox#ANY_CHECKBOX");

		if (IsElementPresent.IsElementPresent(locator1 = "Button#SAVE")) {
			Click.Click(locator1 = "Button#SAVE");
		}
		else if (IsElementPresent.IsElementPresent(locator1 = "Button#UPDATE")) {
			Click.Click(locator1 = "Button#UPDATE");
		}

		Alert.viewSuccessMessage();
	}

	macro gotoEntry {
		Click.Click(
			key_title = "${assetListName}",
			locator1 = "AssetLists#ASSET_LIST_ENTRY_TITLE");
	}

	macro gotoEntryMenuItem {
		LexiconList.clickEllipsisItem(
			item = "${item}",
			listEntry = "${assetListTitle}");
	}

	macro openAssetListsAdmin {
		Navigator.openWithAppendToBaseURL(urlAppend = "group/${siteURLKey}/~/control_panel/manage?p_p_id=com_liferay_asset_list_web_portlet_AssetListPortlet");
	}

	macro openPersonalizedVariation {
		Button.click(button = "New Personalized Variation");

		SelectFrame.SelectFrame(locator1 = "IFrame#MODAL_BODY");
	}

	macro removeScope {
		Click.Click(
			key_title = "${assetListTitle}",
			locator1 = "AssetLists#ASSET_LIST_ENTRY_TITLE");

		Panel.expandPanel(panel = "Scope");

		Click.Click(
			key_scopeName = "${scopeName}",
			locator1 = "AssetLists#ASSET_LIST_REMOVE_SCOPE");

		PortletEntry.save();

		AssertTextNotPresent.AssertTextNotPresent(
			key_scopeName = "${scopeName}",
			locator1 = "AssetLists#ASSET_LIST_SCOPE_NAME",
			value1 = "${scopeName}");

		SelectFrameTop.SelectFrameTop();
	}

	macro renameAssetList {
		AssetListsAdmin.gotoEntryMenuItem(
			assetListTitle = "${oldAssetListTitle}",
			item = "Rename");

		PortletEntry.inputText(
			fieldLabel = "Title",
			text = "${newAssetListTitle}");

		Button.clickSave();
	}

	macro resetCollectionsToCombineAssetsFromSegments {
		ApplicationsMenu.gotoPortlet(
			category = "Configuration",
			panel = "Control Panel",
			portlet = "System Settings");

		SystemSettings.gotoConfiguration(
			configurationCategory = "Assets",
			configurationName = "Collections",
			configurationScope = "System Scope");

		if (IsElementPresent.IsElementPresent(locator1 = "Button#UPDATE")) {
			Click.Click(locator1 = "Icon#HISTORY_VERTICAL_ELLIPSIS");

			MenuItem.clickNoError(menuItem = "Reset Default Values");

			Alert.viewSuccessMessage();
		}
	}

	macro selectAsset {
		var key_assetType = "${assetType}";

		if (isSet(assetListName)) {
			Click.Click(
				key_title = "${assetListName}",
				locator1 = "AssetLists#ASSET_LIST_ENTRY_TITLE");

			WaitForPageLoad.WaitForPageLoad();
		}

		if (IsElementPresent.IsElementPresent(locator1 = "APConfiguration#SOURCE_SCOPE_ASSET_TYPE_SELECT")) {
			AssetListsAdmin.configureItemTypeInManualCollection(
				itemSubtype = "${itemSubtype}",
				itemType = "${itemType}");
		}

		Click.Click(locator1 = "AssetLists#ASSET_LIST_SELECT_BUTTON");

		Click.Click(locator1 = "AssetLists#ASSET_TYPE_DROPDOWN_ITEM");

		SelectFrame.SelectFrame(locator1 = "IFrame#MODAL_BODY");

		if (isSet(depotName)) {
			AssertClick.AssertClick(
				key_breadcrumbName = "Sites and Libraries",
				locator1 = "Breadcrumb#BREADCRUMB_ENTRY",
				value1 = "Sites and Libraries");

			AssertClick.AssertClick(
				key_groupTab = "Asset Library",
				locator1 = "ItemSelector#NAVIGATION_GROUP_TAB",
				value1 = "Asset Library");

			LexiconCard.clickCard(card = "${depotName}");
		}

		if (isSet(entryList)) {
			for (var assetName : list "${entryList}") {
				LexiconEntry.selectEntry(rowEntry = "${assetName}");
			}
		}
		else {
			LexiconEntry.selectEntry(rowEntry = "${assetName}");
		}

		SelectFrameTop.SelectFrameTop();

		Button.clickAdd();

		Alert.viewSuccessMessage();
	}

	macro viewAssetList {
		var key_title = "${assetListTitle}";
		var key_type = "${type}";

		AssertTextEquals.assertPartialText(
			locator1 = "AssetLists#ASSET_LIST_ENTRY_TITLE",
			value1 = "${assetListTitle}");

		AssertTextEquals.assertPartialText(
			locator1 = "AssetLists#ASSET_LIST_ENTRY_TYPE",
			value1 = "${type}");
	}

	macro viewAssetTypeList {
		var key_title = "${assetListName}";

		Click.Click(locator1 = "AssetLists#ASSET_LIST_ENTRY_TITLE");

		if (isSet(manualSelection)) {
			if (isSet(itemType)) {
				Select.Select(
					locator1 = "APConfiguration#SOURCE_SCOPE_ASSET_TYPE_SELECT",
					value1 = "${itemType}");

				if (isSet(itemSubtype)) {
					Select.Select(
						locator1 = "APConfiguration#SOURCE_SCOPE_ASSET_SUBTYPE_SELECT",
						value1 = "${itemSubtype}");
				}

				WaitForElementNotPresent.WaitForElementNotPresent(
					key_text = "Save",
					locator1 = "Button#ANY_DISABLED");

				PortletEntry.save();
			}

			Click.Click(locator1 = "AssetLists#ASSET_LIST_SELECT_BUTTON");

			for (var assetType : list "${assetType}") {
				var key_assetType = "${assetType}";

				AssertElementPresent.AssertElementPresent(locator1 = "AssetLists#ASSET_TYPE_DROPDOWN_ITEM");
			}
		}

		if (isSet(dynamicSelection)) {
			Panel.expandPanel(panel = "Source");

			Click.Click(locator1 = "APConfiguration#SOURCE_SCOPE_ASSET_TYPE_SELECT");

			for (var assetType : list "${assetType}") {
				AssertElementPresent.AssertElementPresent(
					key_assetType = "${assetType}",
					locator1 = "APConfiguration#SOURCE_SCOPE_ASSET_TYPE_OPTION");
			}
		}
	}

	macro viewCollectionItems {
		if (isSet(assetListTitle)) {
			AssetListsAdmin.gotoEntry(assetListName = "${assetListTitle}");
		}

		if (isSet(personalizedVariation)) {
			Navigator.gotoNavTab(navTab = "${personalizedVariation}");
		}

		WaitForLiferayEvent.initializeLiferayEventLog();

		Click.Click(locator1 = "AssetLists#ASSET_LIST_ELLIPSIS");

		Click.Click(
			key_menuItem = "View Items",
			locator1 = "AssetLists#PERSONALIZATION_DROPDOWN_ITEM");

		SelectFrame.SelectFrame(locator1 = "IFrame#MODAL_BODY");

		for (var assetTitle : list "${assetTitleList}") {
			AssertTextEquals.AssertTextEquals(
				key_assetTitle = "${assetTitle}",
				locator1 = "AssetLists#ASSET_TITLE_ROW",
				value1 = "${assetTitle}");
		}

		IFrame.closeFrame();
	}

	macro viewContent {
		Click.Click(
			key_title = "${assetListTitle}",
			locator1 = "AssetLists#ASSET_LIST_ENTRY_TITLE");

		if (isSet(personalizedVariation)) {
			Navigator.gotoNavTab(navTab = "${personalizedVariation}");
		}

		WaitForLiferayEvent.initializeLiferayEventLog();

		Click.Click(locator1 = "AssetLists#ASSET_LIST_ELLIPSIS");

		Click.Click(
			key_menuItem = "View Items",
			locator1 = "AssetLists#PERSONALIZATION_DROPDOWN_ITEM");

		SelectFrame.SelectFrame(locator1 = "IFrame#MODAL_BODY");

		AssertTextEquals.AssertTextEquals(
			key_assetTitle = "${assetTitle}",
			locator1 = "AssetLists#ASSET_TITLE_ROW",
			value1 = "${assetTitle}");

		AssertTextEquals.AssertTextEquals(
			key_assetTitle = "${assetTitle}",
			locator1 = "AssetLists#ASSET_TYPE_ROW",
			value1 = "${assetType}");

		IFrame.closeFrame();
	}

	macro viewNoAssetType {
		var key_title = "${assetListName}";

		Click.Click(locator1 = "AssetLists#ASSET_LIST_ENTRY_TITLE");

		if (isSet(manualSelection)) {
			if (isSet(allAssetsDisabled)) {
				AssertElementNotPresent.AssertElementNotPresent(locator1 = "AssetLists#ASSET_LIST_SELECT_BUTTON");
			}
			else {
				Click.Click(locator1 = "AssetLists#ASSET_LIST_SELECT_BUTTON");

				for (var assetType : list "${assetType}") {
					AssertElementNotPresent.AssertElementNotPresent(
						key_assetType = "${assetType}",
						locator1 = "APConfiguration#SOURCE_SCOPE_ASSET_TYPE_OPTION");
				}
			}
		}

		if (isSet(dynamicSelection)) {
			Panel.expandPanel(panel = "Source");

			Click.Click(locator1 = "APConfiguration#SOURCE_SCOPE_ASSET_TYPE_SELECT");

			for (var assetType : list "${assetType}") {
				AssertElementNotPresent.AssertElementNotPresent(
					key_assetType = "${assetType}",
					locator1 = "APConfiguration#SOURCE_SCOPE_ASSET_TYPE_OPTION");
			}
		}
	}

	macro viewNoContent {
		Click.Click(
			key_title = "${assetListTitle}",
			locator1 = "AssetLists#ASSET_LIST_ENTRY_TITLE");

		if (isSet(personalizedVariation)) {
			Navigator.gotoNavTab(navTab = "${personalizedVariation}");
		}

		WaitForLiferayEvent.initializeLiferayEventLog();

		Click.Click(locator1 = "AssetLists#ASSET_LIST_ELLIPSIS");

		Click.Click(
			key_menuItem = "View Items",
			locator1 = "AssetLists#PERSONALIZATION_DROPDOWN_ITEM");

		SelectFrame.SelectFrame(locator1 = "IFrame#MODAL_BODY");

		var key_assetTitle = "${assetTitle}";

		AssertElementNotPresent.AssertElementNotPresent(locator1 = "AssetLists#ASSET_TITLE_ROW");

		AssertElementNotPresent.AssertElementNotPresent(locator1 = "AssetLists#ASSET_TYPE_ROW");

		IFrame.closeFrame();
	}

	macro viewUsages {
		AssetListsAdmin.gotoEntryMenuItem(
			assetListTitle = "${assetListTitle}",
			item = "View Usages");

		var usageAmount = "${usageAmount}";
		var usageType = "${usageType}";

		AssertElementPresent.AssertElementPresent(locator1 = "AssetLists#ASSET_LIST_USAGES");
	}

}